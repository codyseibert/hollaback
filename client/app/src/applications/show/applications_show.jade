#applications-show
  .container
    .row
      .col-md-12
        h1 {{application.name}}
        
        h2(
          ui-sref="features({applicationId: applicationId})"
        ) View Feature Requests
